%h2 Klienci

= link_to 'Dodaj klienta', new_client_path, class: 'btn btn-default'
%table.table.table-stripped.table-hover
  %tr
    %th id
    %th Imie i nazwisko
    %th Firmy
    %th Więcej
  - @clients.each do |client|
    %tr
      %td= client.id
      %td= client.name
      %td= client.companies.count
      %td= link_to content_tag(:span, '', class: 'glyphicon glyphicon-list-alt'), client_path(client)
